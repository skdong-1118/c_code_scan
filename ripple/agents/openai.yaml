display_name: Ripple
short_description: 使用 CodeGraph MCP 发现代码变更的业务影响流程。
default_prompt: 使用 ripple skill 以交互式流程分析当前分支最后一次提交。先清理 .impact-scan，只检查 HEAD~1..HEAD，根据 git diff 推断 scope，把变更解释为业务语义，再使用 CodeGraph MCP 深入发现顶层业务入口、共享公共流程、业务分叉、跨 subsystem 路径和容易忽略的间接触发流程。每一步都写 Markdown 产物，最后生成中文业务影响报告。
